apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo-service
  namespace: demo-service
  labels:
    app.kubernetes.io/name: demo-service
  annotations:
    # Description for OpenShift console
    openshift.io/description: "Service account for demo-service workloads"
# Note: For external registry access, add imagePullSecrets:
# imagePullSecrets:
#   - name: registry-credentials

apiVersion: v1
kind: Namespace
metadata:
  name: demo-service
  labels:
    # Environment label for policy targeting
    environment: dev
    # Golden path managed
    app.kubernetes.io/managed-by: golden-path
  annotations:
    # OpenShift project description
    openshift.io/display-name: "Demo Service"
    openshift.io/description: "Golden Path demo service deployment"

# Chart.yaml - Template for new services
# This file uses placeholder defaults that pass helm lint.
# Run bootstrap-service.sh to customize for your service.
#
# Placeholders in templates/ use __VAR__ syntax and are replaced
# along with this file when you bootstrap a new service.

apiVersion: v2
name: service-template
description: Golden Path service template (run bootstrap-service.sh to customize)
type: application
version: 0.1.0
appVersion: "1.0.0"

maintainers:
  - name: Platform Team
    email: platform@acme.com

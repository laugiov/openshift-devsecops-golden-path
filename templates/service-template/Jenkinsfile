@Library('golden-path@main') _

// __SERVICE_NAME__ Pipeline
// Generated from Golden Path template
// See: docs/ONBOARDING.md for customization options

goldenPipeline(
    // Required parameters
    appName: '__SERVICE_NAME__',
    buildTool: '__BUILD_TOOL__',           // 'node', 'maven', 'gradle', 'python'

    // Registry configuration
    registry: env.REGISTRY_URL ?: '__REGISTRY_URL__',

    // Security scanning - all enabled by default
    enableSast: true,
    enableSca: true,
    enableSecrets: true,
    failOnSecurityFindings: true,

    // Quality gate
    sonarqubeServer: 'SonarQube',
    sonarqubeQualityGate: __SONARQUBE_ENABLED__,

    // Supply chain security
    enableSbom: true,
    enableSigning: true,
    cosignKeyPath: '../cosign.key',

    // GitOps promotion
    enableGitOps: true,
    gitopsRepo: '__GITOPS_REPO__',
    targetEnv: 'dev',

    // Build configuration
    dockerfile: 'Dockerfile',
    buildContext: '.'

    // Slack notifications (uncomment to enable)
    // slackChannel: '__SLACK_CHANNEL__'
)
